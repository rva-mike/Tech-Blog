
    <div class="card mb-5">
        <div class="card-header">
            <a href="/post/{{id}}" class="text-decoration-none fs-3">{{title}} </a></p>
        </div>
        <div class="card-body">
            <div class="mb-2">
                {{post_content}}
            </div>

            <div class="">
                <b>{{user.username}}</b>
                | {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment"
                    comments.length}}</a>
            </div>
        </div>

    </div>
