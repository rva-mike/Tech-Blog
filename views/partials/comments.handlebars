<div class="comments mt-3">
  {{#each this}}
  <section class="comment card mb-3">
    <div class="meta mb-1 fw-light">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>